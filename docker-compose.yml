services:
  gemini-proxy:
    build: .
    container_name: gemini-proxy
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - gemini-proxy-data:/data
    environment:
      PORT: 8080
      DB_PATH: /data/gemini-proxy.db

volumes:
  gemini-proxy-data:
